<div class="container mt-5">
  <% if @range == "Group" %>
    <div class="row">
      <div class="col-sm-5 offset-1">
        <h2 style="font-weight:bold">検索結果</h2>
      </div>
      <div class="col-sm-5 align-right">
        <%= link_to "新規作成", new_admin_group_path, class: "btn btn-success" %>
      </div>
    </div>
    <div class="row">
      <div class="col-10 mx-auto mt-2">
        <table class="table">
          <thead>
            <tr>
              <th>体育館</th>
              <th>メンバー数</th>
              <th>地域</th>
            </tr>
          </thead>
          <% if @groups.present? %>
            <tbody>
              <% @groups.each do |group| %>
                <tr>
                  <td>
                    <%= link_to admin_group_path(group) do %>
                      <%= group.name %>
                    <% end %>
                  </td>
                  <td><%= group.members.count %></td>
                  <td><%= group.region.name %></td>
                </tr>
              <% end %>
            </tbody>
          <% else %>
            <p>体育館が登録されていません。新規作成ボタンから登録してください。</p>
          <% end %>
        </table>
      </div>
    </div>
  <% else %>
    <div class="row">
      <div class="col-10 mx-auto mt-5">
        <h2 style="font-weight:bold">検索結果</h2>
      </div>
    </div>
    <div class="row">
      <div class="col-10 mx-auto mt-2">
        <table class="table">
          <thead>
            <tr>
              <th>会員ID</th>
              <th>氏名</th>
              <th>メールアドレス</th>
              <th>ステータス</th>
            </tr>
          </thead>
          <tbody>
          <% @members.each do |member| %>
            <tr>
              <td><%= member.id %></td>
              <td>
                <%= link_to admin_member_path(member) do %>
                  <%= member.full_name %>
                <% end %>
              </td>
              <td><%= member.email %></td>
              <td>
                <% if member.is_active %>
                  <span class="text-success">有効</span>
                <% else %>
                  <span class="text-danger">退会</span>
                <% end %>
              </td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% end %>
</div>
